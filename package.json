{
  "name": "loaner",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "start": "node app.js",
  "dev": "nodemon app.js",
  "client": "npm --prefix client run dev",
  "client:build": "npm --prefix client run build",
  "preview": "npm --prefix client run preview",
  "dev:full": "concurrently -n api,web -c blue,green \"nodemon app.js\" \"npm --prefix client run dev\"",
  "build": "npm run client:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "nodemon": "^3.1.7"
  }
}
